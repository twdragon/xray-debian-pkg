#!/bin/bash
set -e
source /usr/share/debconf/confmodule
db_version 2.0

if systemctl list-unit-files | grep -q "^xray" 
then
    if systemctl is-active --quiet "xray"
    then
        deb-systemd-invoke stop xray.service
    fi
fi

#DEBHELPER#

